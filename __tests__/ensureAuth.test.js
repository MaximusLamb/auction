// const { usernamePasswordReader } = require('../lib/middleware/ensureAuth');

// describe('ensureAuth middleware', () => {
//   it('can read a username/password from the header', () => {
//     const authorization = '';
//     expect(usernamePasswordReader(authorization)).toEqual({
//       username: '',
//       password: ''
//     });
//   });
// });
